#!/usr/bin/env bash
set -euo pipefail

rm -rf dist
mkdir -p dist

cp index.html privacy.html terms.html session-01.html favicon.svg _redirects dist/
cp -R assets dist/
python3 scripts/render-source-notes
